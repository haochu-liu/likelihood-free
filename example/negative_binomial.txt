model {
  for (i in 1:N) {
    # 'r' is the size parameter
    # 'p' is the probability parameter: p = 5 / (5 + theta)
    y[i] ~ dnegbin(p, r)
  }

  p <- 5 / (5 + theta)

  theta ~ dgamma(prior_a, prior_b)
}
